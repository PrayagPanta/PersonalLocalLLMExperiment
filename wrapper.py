# from functools import wraps
# from concurrent.futures.thread import ThreadPoolExecutor
#
# def get_parallel_response(func):
#     @wraps(func)
#     def parallel_wrapper(*ars,**kwargs):
#         with ThreadPoolExecutor(max_workers=3) as t:
#             response = t.
#
